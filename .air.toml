# air live-reload config (https://github.com/air-verse/air): `air` rebuilds and
# restarts cmd/server on file changes.
root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  include_ext = ["go"]
  exclude_dir = ["tmp", "examples"]
  delay = 200
